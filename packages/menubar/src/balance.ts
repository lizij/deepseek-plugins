// 余额查询逻辑已迁移至 @deepseek-plugins/shared，此处仅做 re-export 以保持向后兼容。
export { fetchBalance, formatBalance } from '@deepseek-plugins/shared/balance';
export type { BalanceInfo, BalanceResult } from '@deepseek-plugins/shared/balance';
